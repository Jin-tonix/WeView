import 'package:flutter/material.dart';

class PlaceReviewsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("플레이스 리뷰 화면"),
    );
  }
}
